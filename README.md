# chainbridge-pk-export

## Build
Requires go (https://golang.org/doc/install).
```
go build .
```

## Usage
```
./chainbridge-pk-export [keystore-directory] [address]
```
The key will be written to `./privatekey`.